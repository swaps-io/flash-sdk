/**
 * Source of the amount
 *
 * @category Amount Source
 */
export enum AmountSource {
  /**
   * Quote was created using "from" amount as source
   */
  From = 'from',

  /**
   * Quote was created using "to" amount as source
   */
  To = 'to',
}
